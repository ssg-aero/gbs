#include <gtest/gtest.h>
#include <gbs-mesh/halfedgemesh.h>

TEST(gbs_mesh, HalfEdgeVertex)
{
    // gbs::vector<gbs::HalfEdge> edges;
    // gbs::HalfEdgeVertex vtx1;

    // ASSERT_TRUE( vtx1.refHalfEdge == edges.end());
    // ASSERT_NEAR( gbs::get<0>(vtx1.coords),0.,1.e-7);
    // ASSERT_NEAR( gbs::get<1>(vtx1.coords),0.,1.e-7);
    // ASSERT_NEAR( gbs::get<2>(vtx1.coords),0.,1.e-7);

}

TEST(gbs_mesh, add_face)
{
    // using namespace gbs;
    // HalfEdgeMesh msh{};
    // // add_face(
    // //     Real3{0,0,0},
    // //     Real3{2,0,0},
    // //     Real3{1,1,0},
    // //     msh
    // // );

}