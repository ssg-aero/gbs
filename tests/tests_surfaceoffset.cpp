#include <gtest/gtest.h>
#include <gbs/surfaceoffset.h>
#include <gbs/bssurf.h>

using namespace gbs;

TEST(tests_surf_offset, ctor)
{
    using T = double;
    const size_t dim = 3;

    // auto p_srf = std::make_share<BSSurface<T,dim>>(
    //     points_vector<T,dim>
    // )
}